const User = (props) => {
  return (<h2> {props.name} : {props.age}</h2>)

}
 export default User;

 //or

//  export const User = (props) => {
//   return (<h2> {props.name} : {props.age}</h2>)

// }

